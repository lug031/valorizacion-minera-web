/** Proveedor server-side para precios INTER (Fase 1). */
export const INTER_SPOT_PROVIDER_ID = "minted-metal-lbma" as const;

export const INTER_SPOT_PROVIDER_LABEL = "Minted Metal (LBMA spot)";

/** Tiempo máximo de espera al proveedor externo (ms). */
export const INTER_SPOT_FETCH_TIMEOUT_MS = 12_000;
