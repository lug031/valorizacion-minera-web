export function isProvidersEnabledForV1(): boolean {
  return false;
}

/** Listado web de cotizaciones sincronizadas desde móvil (oculto en piloto V1). */
export function isValuationsEnabledForV1(): boolean {
  return false;
}
